# @sdkwork/music-pc-generation

Music generation workspace UI for Playground and other PC surfaces.

## Exports

- `@sdkwork/music-pc-generation/react` — `MusicGenerationWorkspaceView`

## Dependencies

Composition delegates to `@sdkwork/assets-pc-assets/generation-playground-workspace` with `modality="music"` and `bucket="music"`.

## Verification

```bash
pnpm --filter @sdkwork/music-pc-generation typecheck
```
